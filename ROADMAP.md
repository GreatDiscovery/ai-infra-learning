# 从基础 demo 到 AI Infra

每一阶段都以“能运行、能解释、能修改”为完成依据，不以看完材料为依据。

1. **00–01：训练闭环。** Tensor、前向计算、loss、反向传播、优化器、验证集、保存和加载。做一个衣物分类器。
2. **02：解释训练行为。** 在相同初始化和数据顺序下比较学习率，读取逐轮训练/验证指标；用随机标签练习区分记忆与泛化。
3. **03：连接模型与系统。** 固定模型与样本，比较推理 batch、CPU/GPU 和输入预先驻留/逐批搬运。解释完成时间与吞吐。
4. **04：可靠测量。** 先预测矩阵形状与批处理的影响，再用同步计时检验；区分完成延迟与吞吐。
5. **05：定位一个瓶颈。** 用 PyTorch Profiler 记录主机侧准备、搬运、前向与等待；用数值检查和未插桩计时验证预处理向量化。
6. **06：小型推理服务（计划）。** 请求到响应、模型复用、并发与批处理；记录吞吐、延迟与排队成本。
7. **07：大模型推理基础（计划）。** 从小型 Attention 示例理解 Prefill、Decode、KV Cache，再引入真实引擎。

第 00–05 章已可运行，逐章学习即可。第 06–07 章仍为计划；完成当前阶段后再补充。
