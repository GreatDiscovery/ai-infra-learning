# 学习项目协作约定

先读 README.md、PROGRESS.md 和当前章节 README.md，再继续工作。以仓库文件为上下文，不假设能访问另一台电脑上的会话。

- 按小章节推进：先说明一个问题，再给能运行的小 demo，然后引导预测、改一个变量、解释结果。
- 面向机器学习入门学习者，目标逐步走向 AI Infra。新术语第一次出现时用中文解释，优先讲清数据流和训练流程。
- 不把代码已完成、自动测试通过写成学习者已掌握。PROGRESS.md 分开记录工程验证和学习进度。
- 每章有运行命令、预期观察、练习和面试问答。以后章节未实现时明确标记，不一次性扩展大量新主题。
- 默认支持 CPU；能使用时支持 Apple MPS 和 CUDA。无法使用显式指定设备时给出错误，避免悄悄回退。
- 测试以数值正确性、训练与保存/加载闭环为主。性能结论必须附测量条件，不预设 GPU 更快。
- 每次实际学习结束，更新 PROGRESS.md：做了什么、已确认理解什么、剩余问题、下一步及复现命令。只有学习者明确反馈才勾选掌握项。
- 使用相对路径；禁止提交姓名、联系方式、账号凭证、本机用户名、绝对路径、设备唯一标识、简历、聊天原文或其他私人文件。
- data/、artifacts/、results/、private/ 和 .venv/ 仅留本机。不要复制上级目录的内容或 Git 历史。
- 提交前运行 tools/check_share.py 并检查暂存区；只提交当前任务相关的代码和脱敏文档。提交身份用仓库本地配置的通用身份，不使用全局真实姓名或邮箱。
- 新机器先设本仓库 Git 身份，再提交：`git config --local user.name 'AI Infra Learner'` 和 `git config --local user.email 'learner@users.noreply.github.com'`。

建议在另一台电脑这样开始：“请读 AGENTS.md 和 PROGRESS.md，从当前章节继续。先带我理解和运行，不要直接替我完成所有练习。”
